# Cuckc-Fun Setup Guide

## Dependencies (npm)
```bash
npm install
# (Uses lucide-react, react-spring, canvas-confetti, gemini-ai)
```
## Run
```bash
npm run dev
# Vite server at http://localhost:5173
```
## AI Key (Optional)
Create `.env`:
`VITE_GEMINI_API_KEY=your_key_here`
Restart dev server.
